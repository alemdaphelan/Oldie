spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://localhost:5432/oldie_db
    username: alem
    password: 123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
server:
  port: 0